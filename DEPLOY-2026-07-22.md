# Bàn giao deploy — 2026-07-22 (nhánh `vunt38`)

Tổng hợp toàn bộ thay đổi trong phiên làm việc này. Gồm 5 commit, đã push lên `origin/vunt38`:

```
212103e  fix: sua loi Khong tim thay ID du an khi them Hang muc o nhanh Phat trien BIM Tools
01c8d54  feat: /manage cho phep sua/xoa Hang muc 0 viec (Project & BIM Tools), them nut Xuat tong hop o /reports
86260f5  fix: /manage tong quat hoa nhan dien nhom dung CatalogItem (khong chi BT), sua form Xuat tong hop /reports
7505543  docs: them DEPLOY-2026-07-22.md tong hop thay doi session de ban giao deploy
ea70a34  fix: /manage tong quat pre-seed CatalogItem cho moi nhom (khong chi BT), cho luu Loai hinh moi ma chua can Hang muc
```

## Cách deploy

**Không có thay đổi schema DB** — không cần chạy `prisma migrate`/`db push`, không cần backup/thay đổi dữ liệu.

```bash
git fetch origin
git checkout vunt38
git pull origin vunt38
npm install          # nếu package.json/lockfile không đổi thì có thể bỏ qua
npm run build         # prisma generate && next build — tsc chạy kèm, build fail nếu có lỗi type
npm start             # hoặc quy trình restart service hiện có (pm2/systemd/IIS...)
```

Dự án chạy **production build** (`next build` + `next start`), không phải dev server — bắt buộc build lại rồi restart mới thấy thay đổi.

---

## 1. Sửa lỗi "Không tìm thấy ID dự án" khi thêm Hạng mục — nhánh Phát triển BIM Tools

**Nguyên nhân gốc:** hằng số `BT_ABBR` trong `manage-client.tsx` bị hard-code sai `"BT"`, trong khi mã viết tắt thật của nhóm "Phát triển BIM Tools" trong DB là `"PT"`. Khiến toàn bộ logic nhận diện "đây là nhánh BIM Tools" ở trang `/manage` bị vô hiệu — nút "+ Thêm hạng mục" luôn rơi vào luồng Dự án/Project thật, hiện sai dropdown Loại hình và báo lỗi khi lưu.

**Đã sửa:** `BT_ABBR` → `"PT"`; thêm server action `addBtHangMuc()` tạo Hạng mục (CatalogItem level 3) đúng luồng, gắn lại đúng "Dự án BIM Tools" (ProjectGroup tag) để hiện đúng vị trí trong cây.

## 2. /manage — Hạng mục 0 việc: chọn checkbox, sửa tên, xóa trực tiếp

Trước đây Hạng mục không có công việc nào vẫn hiện checkbox nhưng vô tác dụng; nút "Sửa tên hạng mục" chỉ hoạt động với hạng mục có `Project` thật đứng sau (luôn ẩn với Phát triển BIM Tools dù có việc hay không); không có cách xóa hạng mục tại `/manage`.

**Đã thêm:**
- Checkbox ở Hạng mục 0 việc chọn được (danh sách chọn riêng, không lẫn chọn công việc) → thanh thao tác nổi đáy màn hình với **Sửa tên** (chọn đúng 1) và **Xóa** (hàng loạt).
- Icon 🗑 riêng từng dòng để xóa nhanh 1 hạng mục 0 việc.
- Nút bút chì "Sửa tên hạng mục" hoạt động cho cả Hạng mục Project thật **và** CatalogItem.
- Server action mới: `renameBtHangMuc()`, `deleteBtHangMuc()`.

## 3. /manage — Tổng quát hoá nhận diện nhóm dùng CatalogItem (không chỉ Phát triển BIM Tools)

Sau khi sửa mục 1, phát hiện thêm: **Xây dựng HTTC BIM (XD)** và **Đào tạo BIM (DT)** cũng quản lý Hạng mục qua CatalogItem (Khai báo thông tin, Level 1/2/3/5), giống Phát triển BIM Tools — chỉ **QL** và **TT** là 2 nhóm dùng Dự án/Project thật. Do logic trước chỉ nhận diện đúng riêng nhóm Phát triển BIM Tools, XD/DT vẫn bị rơi vào luồng Project sai (dropdown Loại hình hiện toàn bộ danh mục Loại hình công trình global, không lọc theo nhóm).

**Đã sửa:** thêm hàm `catalogWgFor()` — tổng quát hoá điều kiện thành **"mọi nhóm trừ QL/TT"**, áp dụng cho cả nút "+" (cấp Dự án/Loại hình) lẫn logic sửa tên/xóa hạng mục 0 việc ở mục 2.

## 4. /manage — Dropdown "Loại hình" khi thêm Hạng mục → combobox tìm-kiếm-hoặc-tạo-mới

Trước đây dropdown "Loại hình" trong modal "Thêm hạng mục" (cấp Dự án) hiện **toàn bộ danh mục Loại hình công trình toàn hệ thống**, không lọc theo Dự án/Nhóm đang thao tác, và không tạo mới được ngay tại đây.

**Đã sửa:**
- Lọc đúng theo Loại hình đã có trong Dự án đang thao tác (nhóm dùng Project thật) hoặc theo CatalogItem của đúng nhóm (nhóm dùng CatalogItem).
- Đổi `<Select>` → `SearchableCombobox` (component có sẵn, đang dùng ở form thêm công việc inline) — gõ để lọc, gõ tên chưa có sẽ hiện nút "+ Tạo: ..." để tạo Loại hình mới ngay tại đây, áp dụng cho **mọi nhóm công việc**.
- `addBtHangMuc()` sửa thành tự tạo Loại hình (CatalogItem level 2) nếu chưa có thay vì báo lỗi "Không tìm thấy Loại hình".

## 5. /reports — Nút "Xuất tổng hợp" mới + form báo cáo phân cấp

Trang Báo cáo có thêm nút **"Xuất tổng hợp"** (cạnh "Xuất Excel"), dùng đúng dữ liệu đang lọc/sắp xếp trên màn hình. File xuất ra 2 sheet:

- **"Tổng hợp"** — dạng báo cáo phân cấp mô phỏng mẫu **"BÁO CÁO CÔNG VIỆC - PHÒNG BIM"**: số La Mã (I, II, III...) cho Nhóm công việc → số lồng cấp (1.1, 1.1.1, 1.1.1.1) cho Dự án → Loại hình → Hạng mục. Mỗi dòng có:
  - **% hoàn thành** = số việc trạng thái Hoàn thành / tổng số việc trong nhóm đó.
  - **Tiến độ hoàn thành** = ngày Kết thúc chung nếu mọi việc cùng 1 hạn, ngược lại "Theo tiến độ chi tiết từng việc".
  - **Nhân sự thực hiện** = gộp tên người thực hiện khác nhau trong nhóm đó (không tách theo bộ môn/người, tránh đếm trùng công việc).
- **"Chi tiết"** — đầy đủ từng dòng công việc, cùng bộ cột với bảng trên màn hình (Dự án, Loại hình, Hạng mục, Khối hệ thống, Công việc, Giai đoạn, Bộ môn, Thực hiện, Ưu tiên, Tình trạng, Bắt đầu, Kết thúc, Thực tế hoàn thành, Thời gian, Kết quả).

## 6. /manage — Hạng mục 0 việc mới tạo ở Xây dựng HTTC BIM / Đào tạo BIM không hiện trong cây

Sau khi sửa mục 3 (nút "+" đã trỏ đúng CatalogItem của XD/DT), phát sinh tiếp: thêm Hạng mục mới (0 việc) ở 2 nhóm này báo thành công nhưng **không thấy xuất hiện trong cây**.

**Nguyên nhân:** cây `/manage` chỉ hiện Hạng mục 0 việc nhờ cơ chế "pre-seed" từ Khai báo thông tin — cơ chế này (`useBTSeed`) trước chỉ bật riêng cho tab **Phát triển BIM Tools**, không bật cho các nhóm CatalogItem khác.

**Đã sửa:**
- Tổng quát `useBTSeed` → `useCatalogSeed`, bật cho **mọi nhóm dùng CatalogItem** (không chỉ BT).
- Sửa luôn việc xác định đúng cột "Dự án" cho Hạng mục 0 việc: trước luôn dồn về "—" (không có dự án); giờ ưu tiên ProjectGroup gắn thẳng (kiểu BT: TL/QL) rồi tới Level 1 cha của Loại hình (kiểu XD/DT: HTTC...) — Hạng mục mới hiện đúng vị trí trong cây thay vì rơi ra ngoài hoặc mất tích.

## 7. /manage — Modal "Thêm hạng mục": cho lưu Loại hình mới mà chưa cần nhập Hạng mục

Trước đây nút lưu (**"Thêm hạng mục"**) luôn bị khoá (disabled) khi ô "Tên hạng mục" còn trống — kể cả khi người dùng chỉ muốn tạo mới 1 Loại hình rồi thêm Hạng mục sau.

**Đã sửa:**
- Ở nút "+" cấp Dự án (Loại hình chọn tự do), gõ mới 1 Loại hình và **để trống Tên hạng mục** vẫn lưu được — chỉ tạo Loại hình đó, chưa tạo Hạng mục.
- Đổi tên nút **"Thêm hạng mục" → "OK"** (đứng cạnh "Hủy").
- Cấp Loại hình (Loại hình đã cố định sẵn) vẫn bắt buộc nhập Tên hạng mục như cũ (giữ dấu `*`) vì không có gì để "chỉ tạo Loại hình" trong ngữ cảnh đó.

---

## Checklist kiểm tra sau khi deploy

- [ ] `/manage` tab **Phát triển BIM Tools**, **Quản lý BIM**, **Thanh tra BIM**: bấm "+" ở Dự án/Loại hình → thêm Hạng mục thành công, không báo lỗi "Không tìm thấy ID dự án".
- [ ] `/manage` tab **Xây dựng HTTC BIM**, **Đào tạo BIM**: bấm "+" → dropdown "Loại hình" hiện đúng danh mục riêng của nhóm đó (không lẫn Loại hình công trình của QL/TT).
- [ ] Gõ 1 Loại hình chưa có vào combobox → tạo mới thành công, gán đúng cho Hạng mục vừa thêm.
- [ ] Thêm Hạng mục mới (0 việc) ở Xây dựng HTTC BIM / Đào tạo BIM → **hiện ngay trong cây** không cần F5, đúng vị trí Dự án cha.
- [ ] Gõ Loại hình mới, để trống Tên hạng mục, bấm **OK** → chỉ tạo Loại hình, không lỗi, không tạo Hạng mục rác.
- [ ] Hạng mục 0 việc: tích checkbox → thanh thao tác hiện ra → Sửa tên / Xóa hoạt động đúng, không ảnh hưởng hạng mục có việc.
- [ ] `/reports` → nút "Xuất tổng hợp" tải file `.xlsx` 2 sheet đúng như mô tả mục 5, số liệu khớp bộ lọc đang chọn trên màn hình.

## Rollback (nếu cần)

```bash
git revert ea70a34 86260f5 01c8d54 212103e
```

Không có thay đổi schema/dữ liệu nên revert code là đủ, không cần thao tác gì thêm trên DB.
