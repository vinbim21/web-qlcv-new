# Thay đổi — 2026-06-22f

Nhánh: `vunt38`

---

## Có thay đổi schema DB

Thêm cột mới cho bảng `Task`:

- `result String?` — lưu kết quả công việc, có thể là URL hoặc đường dẫn file/thư mục.

Khi deploy môi trường có DB thật, cần cập nhật schema Prisma/DB trước khi chạy app.

---

### 1. Thêm trường "Kết quả" cho công việc

**Ảnh hưởng:** Form thêm/sửa công việc, tab Công việc của tôi, Quản lý công việc, Báo cáo.

- Thêm field `result` vào Prisma `Task`.
- Thêm `result` vào schema validate công việc.
- Form sửa công việc có ô **Kết quả (URL hoặc đường dẫn file)**.
- Server lưu `result` qua `saveTask`.
- Thêm server action `saveTaskResult` để sửa nhanh kết quả ngay trên bảng.

**File:** `prisma/schema.prisma`, `src/lib/schemas/task.ts`, `src/server/actions/tasks.ts`, `src/components/task-row-editor.tsx`

---

### 2. Cột "Kết quả" trong bảng công việc

**Ảnh hưởng:** `/tasks`, `/manage`

- Thêm cột **Kết quả** trong tab **Công việc của tôi** và **Quản lý công việc**.
- Cột rộng `120px`.
- Người được giao hoặc quản lý có thể sửa inline.
- Có nút copy khi hover.
- URL `http://` / `https://` mở tab mới.
- Đường dẫn ổ, UNC path, `file:///...` chỉ hiển thị chữ `Link`, không click mở, nhưng vẫn copy được.

**File mới:** `src/components/result-cell.tsx`  
**File:** `src/app/(app)/tasks/page.tsx`, `src/app/(app)/tasks/tasks-client.tsx`, `src/app/(app)/manage/page.tsx`, `src/app/(app)/manage/manage-client.tsx`

---

### 3. Giới hạn popup chi tiết công việc

**Ảnh hưởng:** Tab **Công việc của tôi**

Trước đây click nhiều vùng trong dòng có thể mở popup chi tiết/ghi giờ. Nay popup chỉ mở khi click vào ô thuộc cột **Công việc**.

**File:** `src/app/(app)/tasks/tasks-client.tsx`

---

### 4. Thêm "Kết quả" vào Báo cáo

**Ảnh hưởng:** `/reports` tab **Danh sách công việc**

- Thêm cột **Kết quả** sau cột **Thực tế hoàn thành**.
- Cột rộng `120px`.
- Dùng chung cách hiển thị/copy link với các tab công việc.
- Cột tham gia tìm kiếm/lọc text.
- Header bảng báo cáo được sticky khi cuộn.

**File:** `src/app/(app)/reports/page.tsx`, `src/app/(app)/reports/report-data.ts`, `src/app/(app)/reports/reports-client.tsx`

---

### 5. Export báo cáo

**Ảnh hưởng:** API export báo cáo

- Query export đã lấy thêm field `result`.

**File:** `src/app/api/export/reports/route.ts`

---

## Kiểm tra

Đã chạy:

```bash
npm run type-check
npm run build
```

Build production pass.

---

## Cách deploy

```bash
git pull origin vunt38
npm install
npx prisma generate
# cập nhật DB cho cột Task.result theo quy trình deploy DB hiện tại
npm run build
pm2 restart qlcv   # hoặc: npm start
```
