# CHANGES-2026-07-10

Tiếp theo commit `d946a56`.

Không có thay đổi schema DB — chỉ cần build lại, không cần `prisma db push`.

---

## 1. Khai báo thông tin — bỏ "Quản lý dự án", "Thêm dự án" nối luôn sang thêm Loại hình/Hạng mục

Áp dụng cho cả 2 tab: **"Dự án"** (chung) và **"Dự án BIM Tools"**.

**Trước:** Có nút riêng "Quản lý dự án" (modal danh sách để đổi tên/xóa/sắp xếp Dự án) + nút "Thêm dự án"/"Thêm hạng mục" chỉ tạo được 1 phần việc mỗi lần (tạo Dự án xong phải tự tìm cách thêm Loại hình/Hạng mục sau).

**Sau:**
- Bỏ hẳn nút **"Quản lý dự án"** và 2 modal `ManageProjectsModal` / `ManagePtProjectsModal` không còn dùng.
- **"Thêm dự án"**: tạo xong Dự án (mã + tên) là **mở luôn modal "Thêm loại hình"** — nhiều dòng, mỗi dòng 1 Loại hình + Hạng mục (Khối/Hệ thống tùy chọn), loại hình gõ mới sẽ tự tạo — cho thêm 1 hoặc nhiều loại hình/hạng mục ngay khi tạo dự án, có thể bỏ qua nếu chưa cần.
- Tab BIM Tools: đổi nút **"Thêm hạng mục" → "Thêm dự án"**, cùng luồng nối tiếp như trên (`AddMultipleBimtoolsModal` đổi từ nhận danh sách `projectGroups` để tự chọn, sang nhận thẳng 1 `group` cố định = dự án vừa tạo).
- **Sửa/Xóa dự án** không mất — chuyển thành icon bút chì/thùng rác:
  - Tab "Dự án": đặt ngay trên dòng nhóm (group header) trong view cây, cạnh nút "+" thêm loại hình có sẵn.
  - Tab "Dự án BIM Tools": đặt cạnh mã dự án trong cột "Dự án" của bảng (hiện khi hover).
  - Xóa vẫn chặn nếu dự án còn hạng mục.

---

## 2. Fix lọc cột "Dự án" ở view cây — ẩn nhầm các dự án chưa có hạng mục

**Lỗi 1 (đã sửa rồi sửa lại):** Khi lọc theo cột nào đó ở view cây "Dự án", dòng tiêu đề nhóm (group header) vẫn hiện **tất cả** các dự án kể cả những dự án có 0 hạng mục khớp bộ lọc — chỉ có danh sách hạng mục con là lọc đúng.

**Sửa lần 1:** Ẩn các dự án có 0 hạng mục khớp khi đang lọc.

**Phát hiện thêm:** Cách sửa lần 1 khiến việc lọc **đích danh cột "Dự án"** (chọn thẳng mã dự án) bị ẩn mất nếu dự án đó **chưa có hạng mục nào cả** — trong khi trường hợp này người dùng đang tìm đúng dự án đó (thường để thêm hạng mục mới cho nó), nên phải luôn hiện.

**Sửa lần 2 (chốt):**
- Lọc đích danh cột **"Dự án"** → dự án được chọn **luôn hiện**, dù chưa có hạng mục nào.
- Lọc theo cột khác (Loại hình / Hạng mục / Khối-Hệ thống) → chỉ hiện dự án có ít nhất 1 hạng mục khớp, như cũ.

---

## Ghi chú build/deploy
- **Không đổi schema DB** — chỉ cần `npm run build` bình thường.
- Chỉ 1 file thay đổi: `src/app/(app)/admin/catalog/catalog-client.tsx`.
- Đã build production (`npm run build`) + `tsc --noEmit` sạch lỗi sau mỗi thay đổi.
