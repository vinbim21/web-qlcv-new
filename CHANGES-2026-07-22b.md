# CHANGES-2026-07-22b

Tiếp theo commit `212103e`.

Không có thay đổi schema DB — chỉ cần build lại, không cần `prisma db push`.

---

## 1. /manage — Hạng mục 0 việc: chọn được checkbox, sửa tên và xóa trực tiếp

**Trước:** ở cây tree (Bảng, gom theo Dự án → Loại hình → Hạng mục), Hạng mục không có công việc nào vẫn hiện checkbox nhưng tích vào không có tác dụng gì (không có việc để chọn). Nút bút chì "Sửa tên hạng mục" chỉ hoạt động với hạng mục có `Project` thật đứng sau — với nhánh **Phát triển BIM Tools** (quản lý qua `CatalogItem`, không qua `Project`) nút này luôn ẩn, dù hạng mục có việc hay không. Không có cách nào xóa hạng mục ngay tại `/manage`.

**Sau:**
- Checkbox ở Hạng mục 0 việc giờ chọn được — tích vào sẽ thêm hạng mục đó vào một danh sách chọn riêng (không lẫn với chọn công việc). Khi có ít nhất 1 hạng mục được chọn, một thanh thao tác nổi ở đáy màn hình hiện ra với nút **Sửa tên** (khi chọn đúng 1 hạng mục) và **Xóa** (xóa hàng loạt).
- Mỗi dòng Hạng mục 0 việc cũng có icon 🗑 riêng để xóa nhanh 1 hạng mục.
- Nút bút chì "Sửa tên hạng mục" sửa lại để nhận diện đúng cả 2 trường hợp: Hạng mục là `Project` thật (đổi tên như cũ) **và** Hạng mục thuộc Phát triển BIM Tools (đổi tên `CatalogItem`, tự đồng bộ sang mọi công việc đang dùng tên cũ).
- Server action mới (`src/server/actions/catalog.ts`): `renameBtHangMuc()` và `deleteBtHangMuc()` — tra `CatalogItem` theo `(workGroupId, level 3, value)` vì phía client không có sẵn id, tái sử dụng `updateCatalogValue()` / `deleteCatalogValue()` đã có (đảm bảo hành vi giống hệt trang Quản trị → Danh mục).
- Xóa Hạng mục Dự án thật dùng `deleteProject()` có sẵn — tự tách liên kết công việc (nếu có), không xóa cứng công việc.

## 2. /reports — Nút "Xuất tổng hợp" mới (cạnh "Xuất Excel")

Trang Báo cáo giờ có 2 nút xuất, cùng dùng đúng dữ liệu đang lọc/sắp xếp trên màn hình (`sorted`):
- **Xuất Excel** — như cũ, danh sách công việc đầy đủ.
- **Xuất tổng hợp** (mới) — file 2 sheet:
  - **"Tổng hợp"**: mỗi dòng là 1 (Nhóm công việc, Đầu việc) — Số hạng mục (đếm hạng mục khác nhau đang chứa đầu việc đó) và Số việc.
  - **"Chi tiết"**: đầy đủ từng dòng công việc, cùng bộ cột với bảng trên màn hình (Dự án, Loại hình, Hạng mục, Khối hệ thống, Công việc, Giai đoạn, Bộ môn, Thực hiện, Ưu tiên, Tình trạng, Bắt đầu, Kết thúc, Thực tế hoàn thành, Thời gian, Kết quả).

Lưu ý: cột "Số việc" đếm số dòng công việc (1 việc = 1 dòng, không nhân theo số người thực hiện) — tránh đếm trùng một công việc nhiều lần.

---

## Ghi chú build/deploy

- Không đổi schema DB.
- Đã build production (`npm run build`) + `tsc --noEmit` + `eslint` sạch lỗi/không cảnh báo mới sau mỗi thay đổi.
- Đã restart server (`npm start`) để áp dụng — dự án chạy production build (`npm run build && npm start`), cần build + restart lại sau mỗi lần đổi code.
