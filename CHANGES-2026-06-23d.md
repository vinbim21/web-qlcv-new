# CHANGES-2026-06-23d

## Reports

- Updated the report task list to display catalog codes instead of names for coded master data:
  - `Du an` uses `ProjectGroup.code`.
  - `Loai hinh` uses `ConstructionType.code`.
  - `Bo mon` uses `Discipline.code`.
  - `Giai doan` uses `Phase.code`.
- BIM Tools fallback now also prefers `ProjectGroup.code` when resolving project data from `CatalogItem`.
- `Hang muc` and `Cong viec` remain names because the current data model does not provide separate codes for those fields.

## Verification

- Ran `npm run build` successfully.
- Restarted production server with `npm start`.
- Verified `/reports` returns HTTP 200.
