# CHANGES-2026-07-22

Tiếp theo commit `5c6c559`.

Không có thay đổi schema DB — chỉ cần build lại, không cần `prisma db push`.

---

## 1. Sửa lỗi "Không tìm thấy ID dự án" khi thêm Hạng mục ở nhánh Phát triển BIM Tools (/manage)

**Nguyên nhân gốc:** hằng số `BT_ABBR` trong `manage-client.tsx` bị hard-code sai thành `"BT"`, trong khi mã viết tắt thật của nhóm "Phát triển BIM Tools" trong DB là `"PT"` (khớp với `admin/catalog/catalog-client.tsx` vốn đã dùng đúng `"PT"`). Vì vậy `btWgId` luôn `null`, khiến toàn bộ logic nhận diện "đây là nhánh BIM Tools" trong cây `/manage` bị vô hiệu.

Hệ quả: bấm nút "+ Thêm hạng mục" ở nhánh Phát triển BIM Tools (Loại hình như Revit/Civil3D/Web App…) luôn rơi vào luồng cũ dành cho Dự án thật (`saveCatalogProject`) — dropdown "Loại hình" hiện sai danh sách (Loại hình công trình chung: Cao tầng, Khách sạn, Nhà ga…) và khi lưu báo lỗi "Không tìm thấy ID dự án" vì nhánh này không có `Project` thật đứng sau (Hạng mục của BIM Tools quản lý qua `CatalogItem`, không qua bảng `Project`).

**Đã sửa:**
- `manage-client.tsx`: `BT_ABBR` → `"PT"` (đúng mã thật trong DB).
- Đổi cách nhận diện nhánh BIM Tools ở 2 nút "+" (cấp Dự án và cấp Loại hình): ưu tiên dò theo `workGroupId` thật của các việc trong nhánh (đáng tin hơn so với chỉ dò theo mã dự án, vì mã dự án của BIM Tools — VD "TL", "QL" — có thể trùng tình cờ với mã ở nhóm khác).
- Khi ở nhánh BIM Tools: dropdown "Loại hình" đổi sang lấy đúng danh sách Loại hình riêng của BIM Tools (Revit, NavisWorks, Cad, Civil3D, Desktop App, Web App…) thay vì Loại hình công trình chung; tiêu đề dialog đổi thành "Thêm hạng mục — Phát triển BIM Tools" cho rõ nghĩa.
- Thêm server action mới `addBtHangMuc()` (`src/server/actions/catalog.ts`): tạo Hạng mục (CatalogItem level 3) đúng luồng — tìm Loại hình (level 2) làm cha, gắn lại đúng "Dự án BIM Tools" (ProjectGroup tag, VD "TL"/"QL") mà người dùng đang thao tác để Hạng mục mới hiện đúng chỗ trong cây, thay vì rơi ra ngoài nhóm "—".

**Lưu ý:** trang **Quản trị → Danh mục → tab "Dự án BIM Tools"** (nút "Quản lý loại hình") đã có sẵn và hoạt động đúng từ trước — không cần tạo thêm tab mới, vấn đề chỉ nằm ở chỗ trang `/manage` chưa trỏ đúng tới nguồn dữ liệu này.

## 2. Sửa lệch dữ liệu `abbr` trong seed script

`prisma/seed.ts` đang khai sai mã viết tắt so với dữ liệu thật trong DB, gây lệch nếu seed lại từ đầu:
- "Phát triển BIM Tools": `"BT"` → `"PT"`.
- "Công việc khác": `"CV"` → `"CK"`.

---

## Ghi chú build/deploy

- Không đổi schema DB.
- Đã build production (`npm run build`) + `tsc --noEmit` sạch lỗi.
- Đã restart server (`npm start`) để áp dụng thay đổi — lưu ý: dự án chạy production build (`npm run build && npm start`), sửa code không tự áp dụng như dev server, cần build + restart lại sau mỗi lần đổi code.
