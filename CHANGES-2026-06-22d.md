# Thay đổi — 2026-06-22d

Nhánh: `vunt38`  
Commits: `f9ac49e` → `671d866` (4 commit so với lần git trước)

---

## DB — cần chạy sau khi pull (một lần)

```bash
npx prisma db push
```

> Thêm enum `TASK_DEADLINE_CHANGE_REQUESTED` và relation `endChangeRequester` trên Task.  
> Không ảnh hưởng dữ liệu cũ.

---

## Thay đổi

### 1. LEVEL_3 (Member) xem được tab Quản lý công việc

Trước đây LEVEL_3 bị redirect sang `/tasks`. Nay mọi user đã đăng nhập đều vào được tab Quản lý.

- Member thấy toàn bộ danh sách **chỉ đọc** — không có checkbox, nút Duyệt/Thêm/Sửa/Xóa.
- Tab "Quản lý công việc" hiện trong sidebar cho tất cả user.

**File:** `manage/page.tsx`, `sidebar-nav.tsx`

---

### 2. Fix filter "Đang làm" trong Quản lý công việc

Bấm thẻ KPI "Đang làm" trước đây chỉ hiện đúng task `DANG_LAM`. Nay giống tab Công việc của tôi: **bỏ HOAN_THANH**, giữ lại Đang làm + Chưa làm + Quá hạn.

**File:** `manage/manage-client.tsx`

---

### 3. Tăng giới hạn bulk action 500 → 2000 ids

Chọn hơn 500 task rồi thao tác hàng loạt trả lỗi Zod `"Too big: expected array to have <=500 items"`. Đã tăng lên 2000 khớp với `take: 2000` của trang Quản lý.

**File:** `src/lib/schemas/task.ts`

---

### 4. Thông báo cho Admin/Quản lý khi user đề xuất dời hạn

Khi member/LEVEL_2 bấm "Đề xuất dời hạn", tất cả **Admin và LEVEL_1** nhận ngay thông báo trên chuông:  
*"Đề xuất dời hạn — [Tên] xin dời: [Tên việc] → dd/mm/yyyy"*

**File:** `prisma/schema.prisma`, `notifications/service.ts`, `actions/tasks.ts`

---

### 5. Hiển thị & duyệt đề xuất dời hạn trong tab Quản lý

- Cột Tình trạng hiện badge cam *"Xin dời → dd/mm/yyyy"* với tooltip tên người đề xuất.
- Khi chọn task có đề xuất, toolbar hiện thêm 2 nút **"Duyệt dời hạn"** / **"Từ chối dời hạn"** — xử lý hàng loạt tất cả task đang chọn có pending.
- Task đề xuất dời hạn được tính vào bộ đếm thẻ KPI **"Chưa giao"** → bấm thẻ là thấy ngay.

**File:** `manage/manage-client.tsx`, `manage/page.tsx`

---

### 6. Navigator kỳ cho tab Công việc và Quản lý

Thay bộ nút kỳ cố định bằng navigator có mũi tên — giống tab Báo cáo.

- Chọn **Tuần / Tháng / Quý / Năm / Tất cả** như cũ.
- Khi chọn kỳ, hiện thanh `< Tuần 26 · 22/06–27/06/2026 >` — bấm `<` `>` để lùi/tiến kỳ.
- Hiện số việc trong kỳ: *T2–T7 · 47/788 việc*.
- **Task quá hạn luôn hiển thị** dù đang xem kỳ nào — không bỏ sót deadline.

**File:** `tasks/tasks-client.tsx`, `manage/manage-client.tsx`

---

## Cách deploy

```bash
git pull origin vunt38
npx prisma db push        # ← BẮT BUỘC lần này
npm run build
pm2 restart qlcv          # hoặc: npm start
```
